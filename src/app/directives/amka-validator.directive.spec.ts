import { AmkaValidatorDirective } from './amka-validator.directive';

describe('AmkaValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AmkaValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
