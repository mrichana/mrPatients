import { BirthdateValidatorDirective } from './birthdate-validator.directive';

describe('BirthdateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new BirthdateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
